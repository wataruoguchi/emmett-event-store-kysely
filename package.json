{
  "name": "poc-emmett-monorepo",
  "type": "module",
  "scripts": {
    "test": "cd example && vitest run",
    "prepare": "husky",
    "format": "biome format --write"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "@types/node": "^20.11.17",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.0",
    "typescript": "^5.8.3"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "biome format"
    ]
  }
}
